# Looking to automate the updates to here
# for the moment:

# on the chainlink repo:
# git pull
# cp -r evm-contracts ../chainlink-brownie-contracts/contracts
# cd ../chainlink-brownie-contracts
# git add *
# git commit -m 'update'
# git push
